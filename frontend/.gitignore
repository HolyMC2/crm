node_modules
# we install with bun but track yarn.lock (upstream's format, and the single
# source of truth); bun migrates it and writes bun.lock as a local artifact
bun.lock
bun.lockb
.DS_Store
dist
dist-ssr
*.local
components.d.ts
# playwright e2e (W6): never commit local secrets or run artifacts
.env
test-results
playwright-report
blob-report
playwright/.cache