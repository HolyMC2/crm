No open questions. Parent approved a minimal successful handoff response when an authorized non-owner assignee reassigns the inquiry away and loses read access: `{name, access_revoked: true}`. The update completes without serializing source, people, or the new owner. Implemented with a database regression; parent coordinates frontend handling.

Resolved: internal hidden immutable capture_payload_hash approved in D006. Parent approved level-1 generic lead-field protection, copy-only serialization redaction, marketing capture guard, and internal assignment notifications in task messages.
