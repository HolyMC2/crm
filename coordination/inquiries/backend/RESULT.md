Backend implementation complete; parent owns final database validation and release decisions.

Files owned and changed:

- `crm/api/inquiries.py`: all seven POST APIs plus the internal, unwhitelisted actor-scoped `find_inquiry_for_request`; explicit responses, input bounds, URL validation, capture-key uniqueness/savepoint replay, immutable payload hashes, optimistic edits, selected-person conversion, target lead permissions, sanitized errors and request-scoped campaign suppression.
- `crm/fcrm/doctype/crm_inquiry/{__init__.py,crm_inquiry.py,crm_inquiry.json}`: parent schema, stored/current locking permission checks, immutable provenance/receipts, copy-only denied-lead response redaction, generic field-level conversion protection, durable internal assignment notifications and content-free after-commit realtime.
- `crm/fcrm/doctype/crm_inquiry_person/{__init__.py,crm_inquiry_person.py,crm_inquiry_person.json}`: stable person keys, explicit roles, conversion receipt fields and standalone child-list denial.
- `crm/hooks.py`: only inquiry parent/child query and document permission registrations.
- `crm/tests/test_inquiries.py`: 28 SQL-backed integration tests with fictional records and mocked outbound mail/realtime/lead assignment delivery.

Contract details communicated to parent/frontend: detail includes `can_write`; people include `lead_accessible`, preserve `converted_at`, and return null for denied lead links. Assignment identities are `{name,full_name}`. A successful reassignment that removes the current actor's read access returns only `{name,access_revoked:true}`. Internal capture hashes are not serialized by the dedicated API. Source text is bounded at 20,000 characters and remains immutable after initial Frappe sanitization. Person/display name bounds are 140, phone 40, email 140, source URL 2048, request ID 200; maximum 50 people and 100 rows per page.

Checks executed by this builder: final Python AST parse of all owned Python plus hooks; JSON parsing and field-order consistency for both DocTypes; whitespace check for hook changes. All passed. Ruff was unavailable (`python -m ruff`: no module); no tool installation or network access was attempted. No commit, deployment, migration or customer send was performed by this builder.

Parent's first database run on a dedicated Frappe+CRM site passed 19/21 tests. The two failed test assumptions were corrected against observed framework behavior: Frappe stamps insert ownership before the controller and sanitizes HTML source content. Subsequent security and workflow additions expanded the suite to 28; the final database rerun is parent-owned and was pending at this handoff. Run the dedicated site's equivalent of `bench --site <dedicated-crm-test-site> run-tests --app crm --module crm.tests.test_inquiries` using the parent's staged candidate and guarded schema path.

Remaining integration checks belong to parent: latest SQL suite, independent two-connection race proofs, marketing source adapter/campaign guard marker/enrollment hook, notification routing/revocation filtering, frontend and browser behavior. Tests intentionally retain user seat limits; use a dedicated site. No optional marketing/ERP app is required to run the core suite.

Runtime followup: parent DB execution identified that top-level `frappe.has_permission` does not accept `print_logs`. All four silent checks now call `frappe.permissions.has_permission` through an explicit import, including the separately authorized correction in `crm/api/notifications.py`. Permission semantics and silent redaction remain intact. Python AST and affected call-site checks pass; no unsupported top-level calls remain in those files. No schema change or candidate restaging was performed by this builder; parent owns the rerun.

Child-query runtime followup: inspected the current installed framework read-only. `frappe.get_list` delegates to `frappe.model.qb_query.DatabaseQuery`; `frappe.database.query.Engine.add_permission_conditions` joins the parent table and applies its permission query conditions when querying a child DocType. Thus authorized child rows are returned even though the legacy child-only hook returns `1=0`. Retained that fallback and replaced the blanket-empty test with `test_generic_child_list_returns_only_authorized_parent_people`: two distinct owners, exact permitted parent rows for owner/assignee/outsider/manager, distinct PII values, and no exposed linked lead ID. Syntax checks pass; requested parent-runner proof after restaging the test and child controller. No schema change.

Known-source followup: added internal, non-whitelisted `capture_source_inquiry(source_key,payload)` and extracted only the shared insert/unique-index recovery into `_save_capture`. Source keys use a separate three-element hash namespace; manual `[actor,request_id]` hashes and public parameters remain unchanged. The helper requires an enabled CRM actor, ordinary insert permission for new captures, and fresh ordinary inquiry read permission for every replay. Authorized replays return the stored snapshot before considering later enrichment; denied actors get no duplicate, owner, or content. Source payload request IDs are optional and do not affect source identity. Five additional SQL tests cover owner/manager replay, denied staff/Guest, namespace isolation/public-key rejection, enrichment immutability, and actual unique-index collision recovery without rolling back prior work. Suite now contains 33 inquiry cases. AST and non-whitelist/public-signature checks pass; parent owns final database proof and marketing adapter integration. No schema change.

Parent acceptance: final 57 core/Forms database checks pass (33 inquiry cases), including the source helper and unchanged permission tests. Four real two-connection proofs pass with candidate hook assertions. See ../FINAL_AUDIT.md for complete integrated evidence and limits.
