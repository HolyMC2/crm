# CRM delivery tasks — 2026-09-09

Source status: locally integrated into the shared branches; see [integration handoff](CRM_INTEGRATION_HANDOFF_2026-09-09.md) and [next-session request](CRM_NEXT_SESSION.md). Historical isolated-candidate evidence below remains valid within its stated limits.

Decision: keep native CRM useful on its own; own the small durable automation core; keep channel providers, AI and n8n optional. Use one CRM shell with app-owned vertical adapters. Compete on reliable inquiry → service → payment workflows before expanding the automation designer.

Execution follow-on: [full feature requirements and Fable decision protocol](CRM_POWERHOUSE_REQUIREMENTS.md) now govern the next build. The approved Fable Wave A assignment produced a reviewed candidate in `/home/holymc2/muelle-worktrees/marketing-bots-20260909`; its `coordination/` files hold status, important questions, Codex decisions and results. The scoped private-marketing-source Fable build is authorized; the earlier source-transfer block is resolved for this assignment.

Inputs: [CRM audit](CRM_ARCHITECTURE_AUDIT_2026-09-09.md), [fresh clinic wiring contract](/home/holymc2/muelle-host/clinica/docs/CRM_VERTICAL_INTEGRATION.md) at Clínica `0c70011`. The clinic document is an architecture proposal, not implemented integration. It corrects the earlier audit's incomplete clinic inventory: Clínica already owns persisted profiles, guarded patient intake, agenda and consented reminders. Reuse those services.

Current completion plan: [complete journeys, contracts and next bounded assignments](CRM_COMPLETION_PLAN_2026-09-09.md). Acquisition ACQ-01/02/03 now has an implemented local candidate and [final audit](../coordination/inquiries/FINAL_AUDIT.md). Automated ingestion and later bot/clinic waves remain open.

## Delivery queue

Each row is an independently reviewable change. “Candidate” means local source and tests, not released. Do not mark browser, migration or delivery acceptance complete from mocked tests.

| ID | Outcome and owner | Depends on | Acceptance / stop condition | State |
| --- | --- | --- | --- | --- |
| CRM-01 | Standalone entry and native records. CRM owns capability boot, routes, menus, optional resources and native status controls. | — | Frappe+CRM has usable Lead/Deal/Contact/Task/Call paths; missing addons never start their RPCs; installed-addon errors do not silently retry mutations through another path. Unsupported silent-send suppression is hidden, ordinary status updates preserved. | Candidate; fresh Frappe+CRM inquiry journey DB/browser proven, broader native-record matrix remains open |
| CRM-02 | Selective upstream Forms and permission fixes. CRM owns patch ledger. | — | Conditional visible/required fields agree in editor, preview and guest submit; link preview restores session after failures; denied linked records stay hidden. Keep patch tests and provenance; no whole upstream merge. | Candidate; broader permission/browser acceptance pending |
| CRM-03 | Shared read-only provider registry. Doco owns protocol; Clínica/Taller own descriptors; CRM owns compiled renderers. | CRM-01 | Core-only, clinic-only, repair-only and both-provider combinations; denied/disabled users; duplicate IDs, bad paths and provider failures; no patient queries or arbitrary component URLs. No shared permission cache. | Candidate tested; both-provider/browser gate open |
| CRM-04 | Wire clinic provisioning into explicit health bundle. Boat/Muelle own install receipts; Abordo carries validated intent; Clínica owns seed method. | CRM-03 | Fictional signup installs Clínica only in health path; consultorio/off and dental/on initialize once; replay preserves admin override; failures show exact step; assets/imports ready. Test both bundle definitions and retail exclusion. No automatic hospital feature claims. | Next, separate provisioning change |
| CRM-05 | Verified relationship + replay-safe conversion. Doco owns restricted relationship/receipt; domain services own writes. | CRM-03 | Explicit link-existing/register-and-link; source lock and unique conversion prevent two actors/two request IDs duplicating; changed-payload retry conflicts; injected failure leaves no orphan Patient/Contact/Customer/Deal. Shared phone never auto-links relatives. | Queued |
| CRM-06 | Permitted clinic schedule and contextual case choice. Clínica agenda remains authority; CRM owns narrow panel. | CRM-05 | Both CRM and source permissions checked for rows/counts; shared-phone/multiple cases requires selection; move/cancel/version/resource rules preserved. No duplicate CRM Event or automatic Deal per booking. | Queued |
| BOT-01 | Make every existing automated send obey current purpose/consent/suppression and explicit human takeover. Marketing owns Chatflow, cadence and send services; Clínica keeps reminder policy. | CRM-01 | Pause/opt-out/takeover while delayed blocks next action; consent lookup error blocks sends; reviewed and automatic sends recheck before transport. Service, marketing and patient-reminder permissions remain distinct. | Wave A candidate: 201 tests + two DB concurrency checks; ownership/version remains B2 gate |
| BOT-02 | Durable action/outbox identity and honest outcomes. Extend existing runs/send logs; reuse clinic dispatch lessons. | BOT-01 | Two workers, retry, crash after provider acceptance and reordered events produce one logical action; uncertain result stays unknown until reconciled; per-tenant claims; all-failed broadcast cannot be labelled successful. Domain write and outbox insert atomic, publication after commit. | Wave A reporting/lock guards validated; durable B1 queued |
| BOT-03 | Versioned native bot execution and optional integration adapter. Marketing owns runtime; CRM owns editor/run history. | BOT-02 | Freeze flow revision per run, bounded waits/retries/action count/cost, explicit bot/human/paused/closed ownership each step, allowlisted scoped actions and audit receipts. First templates: qualify inquiry, assign owner, prepare reply, hand off. n8n/provider outage leaves native CRM/manual work usable. | Queued |
| CRM-07 | Vertical and connection release matrix. App owners supply fictional fixtures; release owner checks UI and direct endpoints. | CRM-01–06, BOT-01–03 for autonomous features | Six presets × allowed/denied users × absent/unconfigured/disconnected/healthy providers. Clinic privacy tests, repair regression, backup/rollback, no customer sends. Publish only features with evidence. | Gate, not blanket certification |
| SOC-01 | Referral capture from Meta mentions or manual source. Marketing owns ingestion; CRM owns triage/lead conversion. | CRM-01; durable receipts from BOT-02 for automated conversion | Verify actual subscriptions and event coverage; alert/assign; preserve source; keep buyer/referrer/additional buyer distinct; deduplicate per event and selected prospect; no customer send. Manual capture remains available for inaccessible group posts. [Observed gap and tasks](CRM_SOCIAL_REFERRAL_AUDIT_2026-09-09.md). | ACQ-01/02/03 candidate tested: native capture, explicit source import, diagnostics. ACQ-04 automatic durable ingestion remains open; production unchanged |

## Foundation scope

The initial review unit covers CRM-01/02/03. Acquisition is the subsequent review unit described below. CRM-03 introduces only `capabilities`, workspace descriptors and module release states. `context`/`execute`, verified patient links, provisioning changes and bot state/schema changes remain separate tasks with explicit acceptance above. This keeps the first change reversible and does not advertise incomplete patient workflows.

Provider discovery returns namespaced contributions from all eligible installed apps. The tenant vertical is a presentation preference. Clínica's current presets remain exactly `consultorio`, `dental`, `clinica`, `hospital`; family medicine can later be a specialty template. Dental stays `demo_only` while enabled; inpatient stays unavailable. Marketing installation and paid plan do not establish provider configuration or user permission.

Clinical notes, diagnoses, images, signed forms and patient histories never enter generic CRM projections, marketing segmentation, browser outbox, logs or realtime payloads. The first contribution navigates to the authorized clinical workspace without passing a patient identity or copying records. Patient reminders keep separate consent and unknown-delivery outcomes.

## Ownership and evidence

Isolated worktrees: `/home/holymc2/muelle-worktrees/{crm-standalone,doco-crm,clinica-crm,taller-crm}-20260909`. Existing active repositories and deployments are preserved. Codex owns architecture, acceptance, integration and final audit. Claude Fable builds the marketing hardening wave in a fifth isolated worktree, `marketing-bots-20260909`, under the written requirements and decision protocol.

Validation: **434 frontend tests / 40 files**, **26 focused Python checks**, successful production asset build with **175 JS/CSS assets verified**, lint/compile/whitespace checks. Read-only staged endpoint smoke passed on actual generic and clinic lab app inventories. Existing upstream Form tests (20) and installed-app API tests (4) also passed in the earlier candidate validation. The full DB call-log permission suite remains blocked by the lab fixture seat cap; no cap bypass was attempted.

Independent Codex review found stale provider cards after settings/role changes; fixed with focus/visible interval refresh, immediate personalized-card clearing and obsolete-response rejection. The initial cross-app audit used Codex after an automatic source-transfer rejection. The subsequent user follow-up authorized the scoped marketing-source Fable assignment, which received approval and produced the Wave A implementation. These are separate runs.

The CRM-01/02/03 foundation and Wave A delivery patch remain separate review units. The foundation introduced no new schema. The later acquisition package adds two CRM DocTypes, installed/migrated only on its dedicated lab site. Neither package includes new patient relationships, provisioning, durable bot runtime or production rollout. Later waves and release gates remain mandatory before claiming complete vertical or autonomous-bot readiness.

## Wave A acceptance

Fable built the delivery hardening patch; Codex reviewed and corrected it, ran 201 passing regressions plus two real-connection checks, and retained the evidence. See [final audit, limits and next bounded assignments](../../doco_marketing/coordination/FINAL_AUDIT.md). Full durable execution, clinic write integration, API/UI recovery and rollout remain open. No builder remains running; no production deployment or migration occurred.


## Acquisition acceptance — ACQ-01/02/03

Implemented a native inquiry inbox with manual URL/text capture, separate requester/referrer/interested people, explicit replay-safe lead create/link, assignment notifications, next action and close/reopen. Direct and generic permissions protect both inquiry content and separately denied lead identities. A source can produce two selected prospects without converting the referrer or enrolling new leads in campaigns. Optional Social Mention capture uses a stable provider-source receipt shared across authorized staff. Read-only Page health distinguishes configuration/subscription evidence from actual delivery and unsupported group-notification coverage.

Validation: **57 core/Forms database tests**, **46 marketing tests**, **470 frontend tests / 46 files**, production build with **178 verified JS/CSS assets**, **four real two-connection checks**, and real desktop/mobile browser acceptance with zero optional-app calls. Fresh dedicated site contains exactly Frappe+CRM; guarded migration logs show no orphan deletion or migration error. Maintenance/scheduler pause remain enabled. See [full audit and retained evidence](../coordination/inquiries/FINAL_AUDIT.md).

Both actual Fable attempts for this wave reached the account limit before source edits. Codex builders completed the written backend/frontend assignments; Codex root integrated marketing, ran acceptance and resolved the independent audit findings. This differs from the earlier Fable-built Wave A. Existing active repositories and production settings remain unchanged.
